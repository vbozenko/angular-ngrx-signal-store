import { Shared } from './shared';

describe('Shared', () => {
  let component: Shared;

  beforeEach(() => {
    component = new Shared();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
